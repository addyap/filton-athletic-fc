# Archive scans

Scanned press cuttings for the "From the archive" pages (`/history/<slug>`).

Files here are served at the site root, e.g. `public/archive/foo.jpg` → `/archive/foo.jpg`.

## Needed

- **`evening-post-2009-bouncing-back.jpg`** — the Bristol Evening Post page
  (20 October 2009, "Filton Athletic are bouncing back to success once again").
  Referenced by `src/data/archiveFeatures.ts`. Until it is added, the page shows
  a placeholder and the full transcription; drop the file in and it appears.

Tip: a JPG around 1200–1600px on the long edge keeps the text legible when
enlarged without bloating the page.
